export enum LoadState {
  Loaded,
  NotLoaded,
  Loading
}

export enum ImageFit {
  WIDTH = 'width',
  HEIGHT = 'height',
  ORIGINAL = 'original'
}

export enum ReaderReadingDirection {
  LeftToRight = 'ltr',
  RightToLeft = 'rtl'
}
