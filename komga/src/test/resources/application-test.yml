application.version: TESTING

komga:
  database:
    file: ":memory:"
  lucene:
    commit-delay: 0s

spring:
  flyway:
    enabled: true
  artemis:
    embedded:
      persistent: false

logging:
  level:
    org.gotson.komga: DEBUG
#    org.jooq: DEBUG
