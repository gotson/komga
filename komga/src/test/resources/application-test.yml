application.version: TESTING

komga:
  database:
    file: ":memory:"

spring:
  datasource:
    url: jdbc:h2:mem:testdb
  flyway:
    enabled: true
  artemis:
    embedded:
      persistent: false

logging:
  level:
    org.jooq: DEBUG
