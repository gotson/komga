application.version: TESTING

komga:
  database:
    file: ":memory:"

spring:
  flyway:
    enabled: true
  artemis:
    embedded:
      persistent: false

logging:
  level:
    org.gotson.komga: DEBUG
#    org.jooq: DEBUG
