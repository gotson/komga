application.version: TESTING

komga:
  database-backup:
    enabled: false

spring:
  datasource:
    url: jdbc:h2:mem:testdb
  flyway:
    enabled: true
  artemis:
    embedded:
      persistent: false

logging:
  level:
    org.jooq: DEBUG
