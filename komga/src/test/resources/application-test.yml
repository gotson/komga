application.version: TESTING

spring:
  flyway:
    enabled: true
    jpa:
      hibernate:
        ddl-auto: validate
  jpa:
    properties:
      hibernate:
        format_sql: true
logging.level.org.hibernate:
  SQL: DEBUG
  type.descriptor.sql.BasicBinder: TRACE