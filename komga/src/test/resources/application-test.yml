application.version: TESTING

spring:
  flyway:
    enabled: false
    jpa:
      hibernate:
        ddl-auto: none
  jpa:
    properties:
      hibernate:
        format_sql: true
logging.level.org.hibernate:
  SQL: DEBUG
  type.descriptor.sql.BasicBinder: TRACE